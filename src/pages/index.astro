---
import Layout from '../layouts/Layout.astro';

// 统计数据
const stats = [
  { number: '3,000+', label: '门课程' },
  { number: '70+', label: '专家讲师' },
  { number: '50+', label: '每月新增课程' },
  { number: '20+', label: '认证课程覆盖领域' }
];

// 解决方案卡片
const solutions = [
  {
    icon: 'building',
    title: '赋能企业，成就未来',
    description: '专为满足组织的独特需求而设计，通过定制化的培训计划和先进的学习管理系统，帮助企业提升员工技能，优化团队绩效，并推动组织文化的发展。'
  },
  {
    icon: 'person',
    title: '激发潜能，引导成长',
    description: '我们的个人解决方案致力于帮助每位学习者实现其职业和个人发展目标。通过精选的课程内容、灵活的学习路径和一对一的职业辅导，我们为个人提供全方位的学习支持。'
  }
];
---

<Layout>
  <!-- Hero Section -->
  <section class="relative bg-gray-50 overflow-hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 md:py-32">
      <div class="text-center">
        <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-gray-900 mb-6">
          成为全球<span class="text-blue-600">受信赖的学习平台</span>
        </h1>
        <p class="text-lg sm:text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
          为每个人提供实现职业目标的工具和资源。我们致力于不断创新，以确保我们的课程内容和教学方法始终处于行业前沿。
        </p>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="bg-blue-600 py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-8">
        {stats.map(stat => (
          <div class="text-center border-r border-blue-500 last:border-r-0">
            <div class="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-2">
              {stat.number}
            </div>
            <div class="text-sm sm:text-base text-blue-100">
              {stat.label}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Mission Section with Image -->
  <section class="py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Image -->
        <div class="order-2 lg:order-1">
          <div class="bg-blue-100 aspect-video flex items-center justify-center">
            <svg class="w-48 h-48 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253M9 12h.01M15 12h.01"></path>
            </svg>
          </div>
        </div>

        <!-- Content -->
        <div class="order-1 lg:order-2">
          <p class="text-base sm:text-lg text-gray-700 leading-relaxed">
            我们是一家多资源的在线教育平台，致力于通过高质量的课程和认证项目，为个人和企业提供终身学习的机会。我们的使命是赋能学习者，无论他们身在何处，都能获得所需的知识和技能，以实现职业发展和个人成长。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Solutions Section -->
  <section class="bg-gray-50 py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-4">
          覆盖企业学习和个人学习解决方案
        </h2>
        <p class="text-lg text-gray-600">
          探索多样化的课程，开启您的学习之旅
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {solutions.map(solution => (
          <div class="bg-white border border-gray-200 p-8 hover:shadow-lg transition-shadow">
            <div class="mb-6">
              {solution.icon === 'building' ? (
                <svg class="w-16 h-16 text-gray-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                </svg>
              ) : (
                <svg class="w-16 h-16 text-gray-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
              )}
            </div>
            <h3 class="text-2xl font-bold text-gray-900 mb-4">
              {solution.title}
            </h3>
            <p class="text-gray-600 leading-relaxed">
              {solution.description}
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Honors Section -->
  <section class="py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-4">
          荣誉与资质
        </h2>
        <p class="text-lg text-gray-600">
          行业认证，质量保证
        </p>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {[1, 2, 3, 4].map(item => (
          <div class="aspect-square bg-gray-100 flex items-center justify-center border border-gray-200 hover:border-gray-300 transition-colors">
            <div class="text-center p-6">
              <div class="text-4xl font-bold text-gray-400 mb-2">
                LOGO
              </div>
              <div class="text-sm text-gray-500">
                认证资质 {item}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
