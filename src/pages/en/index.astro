---
import Layout from '../../layouts/Layout.astro';
import { getLangFromUrl, useTranslations } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// 统计数据
const stats = [
  { number: '3,000+', label: t('home.stats.courses') },
  { number: '70+', label: t('home.stats.instructors') },
  { number: '50+', label: t('home.stats.newCourses') },
  { number: '20+', label: t('home.stats.certFields') }
];

// 解决方案卡片
const solutions = [
  {
    icon: 'building',
    title: t('home.solutions.enterprise.title'),
    description: t('home.solutions.enterprise.desc')
  },
  {
    icon: 'person',
    title: t('home.solutions.individual.title'),
    description: t('home.solutions.individual.desc')
  }
];
---

<Layout>
  <!-- Hero Section -->
  <section class="hero bg-base-200 min-h-[60vh]">
    <div class="hero-content text-center">
      <div class="max-w-4xl">
        <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold mb-6" style="animation: slideInFromLeft 0.6s ease-out">
          {t('home.hero.title')}<span class="text-primary">{t('home.hero.titleHighlight')}</span>
        </h1>
        <p class="text-lg sm:text-xl max-w-3xl mx-auto leading-relaxed opacity-80" style="animation: slideInFromLeft 0.6s ease-out 0.2s both">
          {t('home.hero.subtitle')}
        </p>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="bg-primary py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-0">
        {stats.map((stat, index) => (
          <div
            class={`flex flex-col items-center justify-center text-primary-content py-8 ${index < stats.length - 1 ? 'lg:border-r lg:border-primary-content/20' : ''}`}
            style={`animation: slideInFromLeft 0.5s ease-out ${0.1 * index}s both`}
          >
            <div class="text-3xl sm:text-4xl md:text-5xl font-bold mb-2">{stat.number}</div>
            <div class="text-primary-content/80 text-sm sm:text-base text-center">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Mission Section with Image -->
  <section class="py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Image -->
        <div class="order-2 lg:order-1" style="animation: slideInFromLeft 0.6s ease-out 0.2s both">
          <div class="bg-primary/10 aspect-video flex items-center justify-center">
            <svg class="w-48 h-48 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253M9 12h.01M15 12h.01"></path>
            </svg>
          </div>
        </div>

        <!-- Content -->
        <div class="order-1 lg:order-2" style="animation: slideInFromLeft 0.6s ease-out 0.1s both">
          <p class="text-base sm:text-lg leading-relaxed">
            {t('home.mission')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Solutions Section -->
  <section class="bg-base-200 py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16" style="animation: slideInFromLeft 0.6s ease-out both">
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-4">
          {t('home.solutions.title')}
        </h2>
        <p class="text-lg opacity-80">
          {t('home.solutions.subtitle')}
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {solutions.map((solution, index) => (
          <div class="card card-border bg-base-100" style={`animation: slideInFromLeft 0.6s ease-out ${0.2 + index * 0.15}s both`}>
            <div class="card-body">
              <div class="mb-6">
                {solution.icon === 'building' ? (
                  <svg class="w-16 h-16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                  </svg>
                ) : (
                  <svg class="w-16 h-16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="square" stroke-linejoin="miter" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                  </svg>
                )}
              </div>
              <h3 class="card-title text-2xl mb-4">
                {solution.title}
              </h3>
              <p class="leading-relaxed opacity-80">
                {solution.description}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Honors Section -->
  <section class="py-20 md:py-32">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16" style="animation: slideInFromLeft 0.6s ease-out both">
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-4">
          {t('home.honors.title')}
        </h2>
        <p class="text-lg opacity-80">
          {t('home.honors.subtitle')}
        </p>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {[1, 2, 3, 4].map((item, index) => (
          <div class="card card-border bg-base-100 aspect-square" style={`animation: slideInFromLeft 0.5s ease-out ${0.2 + index * 0.1}s both`}>
            <div class="card-body items-center justify-center text-center">
              <div class="text-4xl font-bold opacity-40 mb-2">
                LOGO
              </div>
              <div class="text-sm opacity-60">
                {t('home.honors.cert')} {item}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
